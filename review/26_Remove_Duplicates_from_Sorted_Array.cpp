#include <iostream>

using namespace std;

int main(){
    int left = 0;
    for (int right = 0; right < array.size(); right++){
        if (array[left]!= array[right]){
            left ++
            array[left] = array[right];
        }

        return (left+1);
    }
}